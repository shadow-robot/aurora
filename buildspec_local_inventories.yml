version: 0.2

env:
  parameter-store:
     AWS_ACCESS_KEY : AKIARFR2MFHY7BRLYNLN
     AWS_SECRET_KEY : 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
phases:
  pre_build:
    commands:
      - echo aws_key=$AWS_ACCESS_KEY and secret=$AWS_SECRET_KEY
      - aws configure set aws_access_key_id $AWS_ACCESS_KEY
      - aws configure set aws_secret_access_key $AWS_SECRET_KEY
  build:
    commands:
      - cd $CODEBUILD_SRC_DIR/ansible/playbooks/molecule_ec2_local_inventories
      - molecule --debug test --all
