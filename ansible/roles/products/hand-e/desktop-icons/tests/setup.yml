---
- name: Delete shadow_hand_launcher_folder
  file:
    path: "{{ shadow_hand_launcher_folder }}"
    state: absent

- name: Delete Shadow_Hand_Launcher.desktop
  file:
    path: "{{ user_folder }}/Desktop/Shadow_Hand_Launcher.desktop"
    state: absent

- name: Delete save_latest_ros_logs_folder
  file:
    path: "{{ save_latest_ros_logs_folder }}"
    state: absent  
    
- name: Delete ROS_Logs_Saver.desktop
  file:
    path: "{{ user_folder }}/Desktop/ROS_Logs_Saver.desktop"
    state: absent  

- name: Delete ROS_Logs_Saver_And_Uploader.desktop
  file:
    path: "{{ user_folder }}/Desktop/ROS_Logs_Saver_And_Uploader.desktop"
    state: absent  

- name: Delete Close_Hand.desktop
  file:
    path: "{{ user_folder }}/Desktop/Close_Hand.desktop"
    state: absent  

- name: Delete Open_Hand.desktop
  file:
    path: "{{ user_folder }}/Desktop/Open_Hand.desktop"
    state: absent  

- name: Delete Demo_Hand.desktop
  file:
    path: "{{ user_folder }}/Desktop/Demo_Hand.desktop"
    state: absent