---
- name: Make sure net-tools are available (arp, ifconfig, netstat, rarp, nameif, route)
  apt:
    name: 'net-tools'
    state: 'present'
    update_cache: yes
  become: yes

- name: Make sure ipaddress (python3) is available
  pip:
    name: 'ipaddress'
  become: yes
