---
- name: Install Docker container
  connection: localhost
  hosts: localhost
  vars_files:
    - vars/docker-deploy.yml
  pre_tasks:
    - name: No product is defined
      when: product != 'hand-e' and product != 'hand-h'
      meta: end_play

    - name: check if customer_key is provided and not false
      when: customer_key is defined and customer_key|bool != false
      set_fact:
        use_aws: true
      
    - name: check if cyberglove branch is provided
      when: cyberglove is defined and cyberglove|bool != false
      set_fact:
        use_cyberglove: true

  roles:
    - { role: installation/docker }
    - { role: installation/nvidia-docker, when: nvidia_docker|bool != false }
    - { role: products/hand-h/deploy, when: product == 'hand-h' }
    - { role: products/hand-e/deploy, when: product == 'hand-e' }
    - { role: aws, when: use_aws }
