---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
  ansible-lint
  flake8
platforms:
  - name: teleop_empty_server_docker
    image: shadowrobot/aurora-test-ubuntu-docker:bionic
    groups:
      - server
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:rw
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: ../../../../roles
  inventory:
    links:
      group_vars: ../../../../inventory/teleop/group_vars
verifier:
  name: testinfra
