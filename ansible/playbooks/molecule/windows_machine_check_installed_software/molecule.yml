---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: teleop_windows_machine_ec2_vm
    groups:
      - windows-machine
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: ../../../roles
  inventory:
    links:
      group_vars: ../../../inventory/teleop/group_vars
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
