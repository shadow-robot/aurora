---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: localhost
    image: ubuntu:xenial-20190222
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: ../../../roles
  inventory:
    links:
      group_vars: group_vars/
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
