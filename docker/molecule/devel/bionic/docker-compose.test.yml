version: '3'

services:
  sut:
    build: .
    command: >
      bash -c "aws --version &&
               test -f /usr/bin/pycharm &&
               test -f /usr/bin/code"
    environment:
      - SOURCE_BRANCH
